rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients have no direct database access. Server access uses IAM.
    match /{document=**} { allow read, write: if false; }
  }
}
